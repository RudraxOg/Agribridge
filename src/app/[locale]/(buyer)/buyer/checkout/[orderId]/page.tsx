import { CheckoutPanel } from "@/components/features/checkout-panel";import { PageHeader } from "@/components/shell/page-header";import { requireLocale } from "@/lib/i18n/locale";
export default async function CheckoutPage({params}:{params:Promise<{locale:string;orderId:string}>}){const {locale:raw,orderId}=await params;const locale=requireLocale(raw);return <><PageHeader eyebrow={`Checkout · ${orderId}`} title="Review and secure order" description="10 t Grade A potato from Awadh Pragati FPC. Buyer and seller charges are shown separately."/><CheckoutPanel locale={locale}/></>}
