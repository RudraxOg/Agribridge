import Link from "next/link";import { PageHeader } from "@/components/shell/page-header";import { StatusChip } from "@/components/shared/status-chip";import { MoneyBreakdown } from "@/components/shared/money-breakdown";import { buttonVariants } from "@/components/ui/button";import { requireLocale } from "@/lib/i18n/locale";
export default async function BuyerOrder({params}:{params:Promise<{locale:string;orderId:string}>}){const {locale:raw,orderId}=await params;const locale=requireLocale(raw);return <><PageHeader eyebrow="Buyer order" title={orderId} description="10 t Grade A potato · Awadh Pragati FPC" actions={<StatusChip status="IN_TRANSIT"/>}/><div className="mb-5 flex flex-wrap gap-3"><Link className={buttonVariants()} href={`/${locale}/shipments/${orderId}`}>Track shipment</Link><Link className={buttonVariants({variant:"secondary"})} href={`/${locale}/settlements/${orderId}`}>View settlement</Link></div><MoneyBreakdown/></>}
