import Link from "next/link"; import { Plus } from "lucide-react"; import { FarmerRegistry } from "@/components/features/farmer-registry"; import { PageHeader } from "@/components/shell/page-header"; import { buttonVariants } from "@/components/ui/button"; import { requireLocale } from "@/lib/i18n/locale";
export default async function FarmersPage({params}:{params:Promise<{locale:string}>}){const locale=requireLocale((await params).locale);return <><PageHeader eyebrow="1,248 farmer members" title="Farmer registry" description="Search verified member records, crop plans and payout history." actions={<Link href={`/${locale}/fpo/farmers/new`} className={buttonVariants()}><Plus size={18}/>Register farmer</Link>}/><FarmerRegistry locale={locale}/></>}
