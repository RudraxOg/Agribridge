import Link from "next/link"; import { Plus } from "lucide-react"; import { CropVisual } from "@/components/features/crop-visual"; import { PageHeader } from "@/components/shell/page-header"; import { StatusChip } from "@/components/shared/status-chip"; import { buttonVariants } from "@/components/ui/button"; import { Card } from "@/components/ui/card"; import { lots } from "@/lib/demo-data"; import { requireLocale } from "@/lib/i18n/locale";
export default async function StockPage({params}:{params:Promise<{locale:string}>}){const locale=requireLocale((await params).locale);return <><PageHeader eyebrow="418 tonnes available" title="Stock management" description="Draft, published, reserved and sold produce from member farmers." actions={<Link href={`/${locale}/fpo/stock/new`} className={buttonVariants()}><Plus size={18}/>Create stock lot</Link>}/><div className="mb-5 flex gap-2 overflow-x-auto pb-1">{["All · 23","Draft · 5","Published · 18","Reserved · 4","Sold · 31","Expiring · 2"].map((x,i)=><button key={x} className={`min-h-11 shrink-0 rounded-xl px-4 text-sm font-bold ${i===0?"bg-[var(--forest)] text-white":"border border-[var(--border)] bg-white"}`}>{x}</button>)}</div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{lots.map(lot=><Link key={lot.id} href={`/${locale}/fpo/stock/${lot.id}`}><Card className="overflow-hidden transition hover:-translate-y-1 hover:shadow-[var(--shadow-md)]"><CropVisual code={lot.image} colour={lot.colour} compact/><div className="p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="text-lg font-black">{lot.name}</h2><p className="text-sm text-[var(--text-muted)]">{lot.variety} · {lot.quantityKg/1000} t</p></div><StatusChip status="Published"/></div><div className="mt-4 flex justify-between border-t border-[var(--border)] pt-4 text-sm"><span>Grade <strong>{lot.grade}</strong></span><strong className="tabular">₹{(lot.pricePaise/100).toFixed(2)}/kg</strong></div></div></Card></Link>)}</div></>}
