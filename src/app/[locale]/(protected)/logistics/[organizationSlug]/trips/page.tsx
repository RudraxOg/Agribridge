import Link from "next/link";
import { MapPin,Navigation,ShieldCheck } from "lucide-react";
import { requirePermission } from "@/lib/auth/require-permission";
import { requireLocale } from "@/lib/i18n/locale";
import { Card } from "@/components/ui/card";
import { buttonVariants } from "@/components/ui/button";
export default async function DriverTripsPage({params}:{params:Promise<{locale:string;organizationSlug:string}>}){const{locale:raw,organizationSlug}=await params;const locale=requireLocale(raw);await requirePermission(locale,organizationSlug,"shipment.position.write");return <main id="main-content" className="mx-auto min-h-dvh max-w-2xl px-4 py-8"><p className="text-xs font-black tracking-[.16em] text-[var(--field)] uppercase">Assigned-driver view · Simulated</p><h1 className="mt-2 text-4xl font-black">Today’s trip</h1><p className="mt-3 text-[var(--text-muted)]">Only the assigned shipment, constrained position updates, and proof controls are available.</p><Card className="mt-7 overflow-hidden"><div className="bg-[var(--forest)] p-6 text-white"><Navigation aria-hidden/><h2 className="mt-5 text-2xl font-black">Gonda → Lucknow</h2><p className="mt-1 text-white/75">Order AB-260914-1072 · Medium goods vehicle</p></div><div className="p-6"><div className="flex gap-3"><MapPin className="shrink-0 text-[var(--forest)]" aria-hidden/><div><strong>Next checkpoint</strong><p className="text-sm text-[var(--text-muted)]">Barabanki toll · 34 km</p></div></div><div className="mt-5 flex gap-3 rounded-xl bg-[var(--surface-muted)] p-4"><ShieldCheck className="shrink-0 text-[var(--forest)]" aria-hidden/><p className="text-sm">GPS updates are demo-only and never claim live provider tracking.</p></div><Link href={`/${locale}/shipments/AB-260914-1072`} className={`${buttonVariants()} mt-6 w-full`}>Open assigned trip</Link></div></Card></main>}
