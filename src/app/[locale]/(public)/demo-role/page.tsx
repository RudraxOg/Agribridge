import Link from "next/link";
import { ArrowLeft, Sprout } from "lucide-react";
import { RoleSwitcher } from "@/components/features/role-switcher";
import { requireLocale } from "@/lib/i18n/locale";
export default async function DemoRolePage({ params }: { params: Promise<{ locale: string }> }) { const locale = requireLocale((await params).locale); return <main id="main-content" className="mx-auto min-h-dvh max-w-6xl px-4 py-8 md:px-8 md:py-14"><Link href={`/${locale}`} className="inline-flex min-h-12 items-center gap-2 font-bold text-[var(--forest)]"><ArrowLeft aria-hidden size={18} />Back to AgriBridge</Link><div className="mt-8 mb-10"><span className="grid size-14 place-items-center rounded-2xl bg-[var(--forest)] text-white"><Sprout aria-hidden /></span><p className="mt-7 text-xs font-black tracking-[.16em] text-[var(--field)] uppercase">Guided prototype</p><h1 className="mt-2 text-4xl font-black tracking-tight md:text-5xl">Choose your working view</h1><p className="mt-3 max-w-2xl text-lg text-[var(--text-muted)]">A controlled demo session stores your selected role in a secure cookie. Changing only the URL does not grant a production role.</p></div><RoleSwitcher locale={locale} /></main>; }
