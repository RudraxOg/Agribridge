import { cookies } from "next/headers";import { AppShell } from "@/components/shell/app-shell";import { requireLocale } from "@/lib/i18n/locale";
export default async function SharedLayout({children,params}:{children:React.ReactNode;params:Promise<{locale:string}>}){const locale=requireLocale((await params).locale);const saved=(await cookies()).get("agribridge_demo_role")?.value;const role=saved==="buyer"?"buyer":"fpo";const items=role==="buyer"?[{label:"Marketplace",path:"/buyer/marketplace",icon:"marketplace" as const},{label:"Orders",path:"/buyer/orders",icon:"orders" as const},{label:"Logistics",path:"/shipments/AB-260914-1072",icon:"logistics" as const},{label:"Payments",path:"/buyer/payments",icon:"payments" as const}]:[{label:"Overview",path:"/fpo/overview",icon:"overview" as const},{label:"Farmers",path:"/fpo/farmers",icon:"farmers" as const},{label:"Stock",path:"/fpo/stock",icon:"stock" as const},{label:"Orders",path:"/fpo/orders",icon:"orders" as const}];return <AppShell locale={locale} role={role} items={items}>{children}</AppShell>}
