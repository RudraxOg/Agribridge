import { BellRing, CheckCircle2, Truck, Video } from "lucide-react";import { PageHeader } from "@/components/shell/page-header";import { Card } from "@/components/ui/card";
export default function NotificationsPage(){const notes=[[Truck,"Shipment on schedule","UP 43 AT 2186 is 64 km from Lucknow","2 min ago"],[Video,"Grading call reminder","Potato lot inspection starts at 3:30 PM","34 min ago"],[CheckCircle2,"First release recorded","₹94,250 added to the FPO settlement balance","1 hr ago"]];return <><PageHeader eyebrow="3 unread" title="Notifications" description="Operational updates and actions across your current organization."/><Card className="divide-y divide-[var(--border)] overflow-hidden">{notes.map(([Icon,title,body,time])=>{const I=Icon as typeof BellRing;return <article className="flex gap-4 p-5" key={String(title)}><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-[var(--surface-muted)] text-[var(--forest)]"><I/></span><div><h2 className="font-black">{String(title)}</h2><p className="text-sm text-[var(--text-muted)]">{String(body)}</p><p className="mt-2 text-xs font-bold text-[var(--field)]">{String(time)}</p></div></article>})}</Card></>}
