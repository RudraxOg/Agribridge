import Link from "next/link";
import { ArrowLeft,LockKeyhole } from "lucide-react";
import { buttonVariants } from "@/components/ui/button";
export function AccessDeniedState({locale,reason}:{locale:string;reason?:string}){return <main id="main-content" className="grid min-h-dvh place-items-center px-4"><div className="max-w-lg text-center"><span className="mx-auto grid size-16 place-items-center rounded-2xl bg-[var(--surface-muted)] text-[var(--forest)]"><LockKeyhole aria-hidden/></span><p className="mt-6 text-xs font-black tracking-[.16em] text-[var(--field)] uppercase">Access not available</p><h1 className="mt-2 text-4xl font-black">This workspace is outside your permissions</h1><p className="mt-4 text-[var(--text-muted)]">Your identity is signed in, but an active membership and the required permission are both checked before access. Reference: {reason??"authorization"}.</p><div className="mt-7 flex flex-wrap justify-center gap-3"><Link className={buttonVariants()} href={`/${locale}/organizations`}>Choose organization</Link><Link className={buttonVariants({variant:"secondary"})} href={`/${locale}/help`}><ArrowLeft aria-hidden size={18}/>Request access help</Link></div></div></main>}
