"use client";
import { useActionState } from "react";
import { createOrganizationAction } from "@/features/auth/onboarding-actions";
import { initialAuthState } from "@/features/auth/actions";
import { Button } from "@/components/ui/button";
import { Field,Input,Select } from "@/components/ui/form-controls";
export function OnboardingForm({locale}:{locale:string}){const[state,action,pending]=useActionState(createOrganizationAction,initialAuthState);return <form action={action} className="grid gap-5"><input type="hidden" name="locale" value={locale}/><Field label="Organization type"><Select name="type" defaultValue="fpo"><option value="fpo">Farmer Producer Organization</option><option value="buyer">Bulk buyer</option><option value="logistics">Logistics partner</option></Select></Field><Field label="Registered or working name"><Input name="name" autoComplete="organization" required/></Field><Field label="Workspace URL" hint="Lowercase letters, numbers and hyphens only."><div className="flex items-center rounded-xl border border-[var(--border)] bg-white pl-3 text-sm text-[var(--text-muted)]"><span>agribridge/</span><Input className="border-0" name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" required/></div></Field><div className="grid gap-5 sm:grid-cols-2"><Field label="State"><Input name="state" required/></Field><Field label="District"><Input name="district" required/></Field></div>{state.status!=="idle"&&<p role="alert" className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-900">{state.message}</p>}<Button disabled={pending}>{pending?"Creating workspace…":"Create verified workspace"}</Button><p className="text-xs text-[var(--text-muted)]">Creation and owner membership happen in one database transaction. In production this requires a verified email.</p></form>}
