import { Leaf } from "lucide-react";
export function CropVisual({ code, colour, compact = false }: { code: string; colour: string; compact?: boolean }) { return <div className={`field-lines grain relative overflow-hidden bg-[var(--forest)] ${compact ? "h-32" : "h-52"}`}><div className="absolute -right-6 -bottom-10 size-40 rounded-full border-[18px] border-white/10" /><div className="absolute top-5 right-5 grid size-16 place-items-center rounded-full bg-white/10 text-white"><Leaf aria-hidden size={30} /></div><div className="absolute bottom-4 left-4"><span className="text-xs font-black tracking-[.22em] text-white/65">HARVEST LOT</span><p className="mt-1 text-2xl font-black text-white" style={{ textShadow: `0 3px 20px ${colour}` }}>{code}</p></div></div>; }
