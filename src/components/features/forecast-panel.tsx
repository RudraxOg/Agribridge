"use client";
import { useState } from "react";
import { Info, TrendingUp } from "lucide-react";
import { forecastRows } from "@/lib/demo-data";
import { Card } from "@/components/ui/card";
export function ForecastPanel(){const [days,setDays]=useState<15|30>(15);return <><div className="mb-5 flex gap-2"><button onClick={()=>setDays(15)} className={`min-h-12 rounded-xl px-5 font-bold ${days===15?"bg-[var(--forest)] text-white":"border border-[var(--border)] bg-white"}`}>15 days</button><button onClick={()=>setDays(30)} className={`min-h-12 rounded-xl px-5 font-bold ${days===30?"bg-[var(--forest)] text-white":"border border-[var(--border)] bg-white"}`}>30 days</button><select className="min-h-12 rounded-xl border border-[var(--border)] bg-white px-4" aria-label="Crop"><option>All crops</option><option>Potato</option><option>Basmati paddy</option></select></div><div className="grid gap-5 lg:grid-cols-3">{forecastRows.map(row=><Card className="overflow-hidden" key={row.crop}><div className="field-lines bg-[var(--forest)] p-5 text-white"><div className="flex justify-between"><TrendingUp/><span className="text-xs font-bold">{row.confidence}% confidence</span></div><h2 className="mt-5 text-2xl font-black">{row.crop}</h2><p className="text-sm text-white/70">{row.district} · next {days} days</p></div><div className="grid gap-3 p-5 text-sm"><div className="flex justify-between"><span className="text-[var(--text-muted)]">Expected price</span><strong className="tabular">{row.range}</strong></div><div className="flex justify-between"><span className="text-[var(--text-muted)]">Demand</span><strong>{row.demand}</strong></div><div className="flex justify-between"><span className="text-[var(--text-muted)]">Supply pressure</span><strong>{row.pressure}</strong></div><div className="flex justify-between"><span className="text-[var(--text-muted)]">Harvest window</span><strong>{row.window}</strong></div><div className="mt-2 rounded-xl bg-[var(--surface-muted)] p-3"><strong>Suggested segment</strong><p className="text-[var(--text-muted)]">{row.buyer}</p></div></div></Card>)}</div><div className="mt-5 flex gap-3 rounded-xl border border-[var(--info)]/25 bg-blue-50 p-4 text-sm"><Info className="shrink-0 text-[var(--info)]"/><p><strong>Explainable baseline only.</strong> Recent modal-price moving averages are compared with the previous window, then adjusted using seeded seasonality and demand factors. Prices are not guaranteed. Demo source synced 14 Sep 2026, 06:00 IST.</p></div></>}
