"use client";
import Link from "next/link";import { Radio, ShoppingBasket } from "lucide-react";import { toast } from "sonner";import { Button, buttonVariants } from "@/components/ui/button";
export function ProductActions({locale,lotId,basePath="/buyer"}:{locale:string;lotId:string;basePath?:string}){return <div className="sticky bottom-20 z-20 flex gap-2 rounded-2xl border border-[var(--border)] bg-white p-3 shadow-[var(--shadow-md)] lg:static lg:border-0 lg:bg-transparent lg:p-0 lg:shadow-none"><Button className="flex-1" variant="secondary" onClick={()=>toast.success("Mock grading call requested for tomorrow at 11:00 AM")}><Radio size={18}/>Request call</Button><Link className={`${buttonVariants()} flex-1`} href={`/${locale}${basePath}/checkout/AB-260914-1072?lot=${lotId}`}><ShoppingBasket size={18}/>Add to order</Link></div>}
