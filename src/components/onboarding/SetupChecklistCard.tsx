import { CheckSquare } from "lucide-react";
import Link from "next/link";
import { Card } from "@/components/ui/card";
import type { SetupChecklistItem } from "@/features/onboarding/server/get-setup-checklist";
export function SetupChecklistCard({items}:{items:SetupChecklistItem[]}){const done=items.filter(item=>item.state==="completed").length;return <Card className="p-5"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-[var(--surface-muted)] text-[var(--forest)]"><CheckSquare aria-hidden/></span><div><p className="text-xs font-black tracking-[.12em] text-[var(--field)] uppercase">First-time setup</p><h2 className="font-black">{done} of {items.length} complete</h2></div></div><ul className="mt-5 grid gap-2">{items.map(item=><li key={item.key} className="rounded-xl bg-[var(--surface-muted)] px-3 py-3"><div className="flex items-start gap-3"><span aria-hidden className={`mt-0.5 grid size-5 shrink-0 place-items-center rounded-full text-xs font-black ${item.state==="completed"?"bg-[var(--action)] text-white":item.state==="blocked"?"bg-[var(--warm-sand)] text-[var(--earth)]":"border border-[var(--border)] bg-white text-[var(--text-muted)]"}`}>{item.state==="completed"?"✓":item.state==="blocked"?"!":""}</span><div className="min-w-0 flex-1"><p className="font-bold">{item.label}</p><p className="mt-0.5 text-xs text-[var(--text-muted)]">{item.detail}</p>{item.href&&<Link href={item.href} className="mt-2 inline-flex min-h-11 items-center text-sm font-bold text-[var(--forest)]">Continue →</Link>}</div><span className="text-xs font-bold uppercase text-[var(--text-muted)]">{item.state}</span></div></li>)}</ul></Card>}
