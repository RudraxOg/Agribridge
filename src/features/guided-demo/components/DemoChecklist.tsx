export function DemoChecklist({items}:{items:Array<{label:string;done:boolean}>}){return <ul className="grid gap-2">{items.map(item=><li key={item.label} className="flex items-center gap-3 rounded-xl bg-[var(--surface-muted)] px-3 py-2 text-sm"><span aria-hidden className={`grid size-5 place-items-center rounded-full text-xs font-black ${item.done?"bg-[var(--action)] text-white":"border border-[var(--border)] bg-white text-[var(--text-muted)]"}`}>{item.done?"✓":""}</span>{item.label}</li>)}</ul>}
