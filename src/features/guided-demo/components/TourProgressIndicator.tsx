export function TourProgressIndicator({current,total}:{current:number;total:number}){const progress=total?Math.round((current/total)*100):0;return <div aria-label={`Step ${current} of ${total}`}><div className="flex justify-between text-xs font-bold text-[var(--text-muted)]"><span>Step {current} of {total}</span><span>{progress}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-[var(--surface-muted)]"><div className="h-full rounded-full bg-[var(--action)] transition-[width] duration-300 motion-reduce:transition-none" style={{width:`${progress}%`}}/></div></div>}
