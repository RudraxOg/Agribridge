import type { Forecast,ForecastProvider } from "./forecast-provider";import type { MarketPrice } from "@/integrations/market-data/market-data-provider";
export class BaselineForecastProvider implements ForecastProvider{forecast(prices:MarketPrice[],days:15|30):Forecast{if(!prices.length)throw new Error("At least one market price is required");const avg=prices.reduce((n,p)=>n+p.modalPrice,0)/prices.length;const spread=Math.max(avg*.08,prices.reduce((n,p)=>n+(p.maxPrice-p.minPrice),0)/prices.length/2);return{commodity:prices[0].commodity,days,expectedMin:Math.round(avg-spread),expectedMax:Math.round(avg+spread),confidence:prices.length>5?82:68,direction:"stable",drivers:["Recent modal-price moving average","Seasonal harvest window","Seeded buyer-demand factor"],sourceTimestamp:prices[0].syncedAt}}}
