import type { IdentityProvider,IdentityResult } from "./identity-provider";export class MockIdentityProvider implements IdentityProvider{async verify(reference:string,consentRecordId:string):Promise<IdentityResult>{void consentRecordId;const lastFour=reference.slice(-4).padStart(4,"0");return{provider:"mock-offline-identity",verificationReference:`mock_${lastFour}`,maskedIdentifier:`•••• •••• ${lastFour}`,lastFour,status:"verified",verifiedAt:"2026-09-14T00:30:00.000Z"}}}
