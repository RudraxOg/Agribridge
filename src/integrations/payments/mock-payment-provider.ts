import type { PaymentProvider } from "./payment-provider";export class MockPaymentProvider implements PaymentProvider{async createOrder(input:{orderId:string;amountPaise:bigint}){return{providerReference:`mock_pay_${input.orderId}`,status:"succeeded" as const,amountPaise:input.amountPaise}}async refund(input:{providerReference:string;amountPaise:bigint}){return{providerReference:`mock_refund_${input.providerReference}`,status:"succeeded" as const,amountPaise:input.amountPaise}}}
