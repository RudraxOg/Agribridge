import type { VoiceProvider } from "./voice-provider";export class BrowserVoiceProvider implements VoiceProvider{async speak(text:string,locale:string){if(!("speechSynthesis" in window))throw new Error("Speech synthesis unsupported");const utterance=new SpeechSynthesisUtterance(text);utterance.lang=locale;speechSynthesis.speak(utterance)}async listen(locale:string):Promise<never>{void locale;throw new Error("Speech recognition is not supported by this browser adapter")}}
