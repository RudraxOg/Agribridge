import { z } from "zod";import { quantityUnits } from "./constants";export const stockLotSchema=z.object({commodity:z.string().min(2),variety:z.string().optional(),quantity:z.number().positive(),unit:z.enum(quantityUnits),unitPricePaise:z.bigint().positive(),contributorTotal:z.number().positive()}).refine(x=>x.contributorTotal===x.quantity,{message:"Contributor allocations must exactly match lot quantity"});
