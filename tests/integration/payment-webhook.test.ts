import { createHmac } from "node:crypto";import { describe,expect,it } from "vitest";import { verifyWebhookSignature,WebhookIdempotency } from "@/integrations/payments/webhook-security";
describe("payment webhook security",()=>{it("validates the provider signature",()=>{const body='{"event":"payment.captured"}';const signature=createHmac("sha256","secret").update(body).digest("hex");expect(verifyWebhookSignature(body,signature,"secret")).toBe(true);expect(verifyWebhookSignature(body,"bad","secret")).toBe(false)});it("rejects duplicate event processing",()=>{const store=new WebhookIdempotency();expect(store.claim("evt-1")).toBe(true);expect(store.claim("evt-1")).toBe(false)})});
