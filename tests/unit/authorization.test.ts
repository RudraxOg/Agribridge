import {describe,expect,it} from "vitest";
import {hasPermission,permissions,roleHome} from "@/lib/authorization/permissions";
import {safeRedirectPath} from "@/lib/auth/redirects";
describe("authorization helpers",()=>{it("checks granular permission sets",()=>{expect(hasPermission(new Set([permissions.lotsRead]),permissions.lotsRead)).toBe(true);expect(hasPermission([permissions.lotsRead],permissions.farmersWrite)).toBe(false)});it("routes drivers away from the full dispatch dashboard",()=>{expect(roleHome("en","logistics_driver","demo-logistics")).toBe("/en/logistics/demo-logistics/my-trips");expect(roleHome("hi","logistics_dispatcher","demo-logistics")).toBe("/hi/logistics/demo-logistics/dispatch")});it("routes finance roles to finance-safe canonical homes",()=>{expect(roleHome("en","fpo_finance","awadh")).toBe("/en/fpo/awadh/settlements");expect(roleHome("hi","buyer_finance","lucknow")).toBe("/hi/buyer/lucknow/payments")});it("keeps platform roles on separate least-privilege homes",()=>{expect(roleHome("en","platform_admin")).toBe("/en/platform/admin");expect(roleHome("en","compliance_auditor")).toBe("/en/platform/audit");expect(roleHome("en","support_agent")).toBe("/en/platform/support")});it("rejects cross-origin and protocol-relative redirects",()=>{expect(safeRedirectPath("//evil.example/steal","/en")).toBe("/en");expect(safeRedirectPath("https://evil.example/steal","/en")).toBe("/en");expect(safeRedirectPath("/en/fpo/overview?tab=stock","/en")).toBe("/en/fpo/overview?tab=stock")})});
